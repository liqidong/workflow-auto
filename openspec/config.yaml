schema: spec-driven

context: |
  Repository: workflow-auto
  Purpose: host the reusable workflow-base template
  Workflow posture: thin-routing, heavy-evidence
  Canonical routing surface: .agents/skills/goi-workflow/SKILL.md
  Validation chain:
    - scripts/check-host-workflow-deps.sh
    - scripts/verify-workflow-template.sh
    - ./.venv/bin/pytest -q
    - openspec validate --specs
